@* Component for Rendering Guitar Strings *@
@using FinalYearProject.Shared.Models.TabRepresentation
<g>
    @for (int i = 0; i < 6; i++)
    {
        // Draw each string line
        <line x1="0"
              y1="@(i* StringSpacing + _topPadding)"
              x2="@(_width)"
              y2="@((i * StringSpacing) + _topPadding)"
              stroke="black" stroke-width="1" />
    }
</g>

@* Show the End of the Bar *@
<BarLine StartYPosition="_topPadding"
         EndYPosition="@(5 * StringSpacing + _topPadding)"
         XPosition="_width" />
