<g>

    <circle cx="@(LeftPadding)"
            cy="@(TopPadding - (NoteCutoutRadius / 2))"
            r="@NoteCutoutRadius"
            fill="white" />

    <text x="@LeftPadding"
          y="@TopPadding"
          font-family="Arial"
          font-size="12"
          fill="black"
          text-anchor="middle">3</text>
</g>

