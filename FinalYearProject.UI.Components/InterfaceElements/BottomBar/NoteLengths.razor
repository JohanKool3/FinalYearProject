@using FinalYearProject.UI.Components.InterfaceElements.BottomBar.NoteDuration
@* Here will be the note lengths *@

@{
    var note = BarInformation.Notes[0];
}

<CrotchetDuration XPosition="@GetNoteXPosition(note)" />