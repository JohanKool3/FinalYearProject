@if (_isVisible)
{
    <!-- Playback line -->
    <line x1="@_playbackXPosition"
          y1="@YPosition"
          x2="@_playbackXPosition"
          y2="@(YPosition + _height)"
          stroke="@_color"
          stroke-width="@_width"
          class="playback-line" />
}